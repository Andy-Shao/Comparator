1,host_system_id,int,not null
2,account_id,varchar(20),not null
3,balance_date,datetime,not null
4,orig_ccy_balances,float,not null

##END!!

SELECT 
host_system_id , 
account_id , 
balance_date , 
orig_ccy_balances
FROM txn_account_balances WHERE host_system_id IN (${handoff_source_id})

##END!!